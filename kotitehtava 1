#include<iostream>
#include<cstdlib>
#include<ctime>
using namespace std;

/*
int main() {

  srand(1);
  // generate random number
  int random = rand();

  cout << random/2<<endl;

return 0;
}
*/

/*
int main() {
  srand((unsigned) time(0));
  int randomNumber;
  for (int index = 0; index < 1; index++) {
    randomNumber = (rand() % 20) + 1;
    cout << randomNumber << endl;
  }
}

*/




int main(){

 //set seed
 srand((unsigned) time(0));


 int randomNumber;
 int guess;
 int tries = 0;


    for(int i = 0; i < 1; i++)
    {
        randomNumber=(rand() % 20)+1;
    }

    cout<<randomNumber<<endl;
    cin>>guess;
    cout<<"Arvaamasi numero:"<<guess<<endl;

    while(guess != randomNumber && tries < 20 ){
        cin>>guess;
        tries ++;

      if(guess > randomNumber)
      {
          cout<<"Luku on pienempi"<<endl;
          //return;
      }
      if(guess < randomNumber)
      {
          cout<< "Luku on suurempi"<<endl;
          //return;
      }

      if (guess == randomNumber)
      {
          cout<<"Oikea vastaus!"<<endl;

      }
   }
    return 0;
}
